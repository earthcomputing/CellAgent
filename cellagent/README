Assuming this file is in top level of CellAgent repository.
An environment variable CELL_AGENT_DIR must be set.

To load new driver:
Edit (using sudo) /etc/modprobe.d/e1000e.conf to uncomment "blacklist e1000e".
sudo depmod -ae && sudo update-initramfs -u
Then reboot.  RECOMMENT THE LINE IN /etc/modprobe.d/e1000e.conf AND RERUN ABOVE LINE WHEN DONE!

After turning on cell:
$ sudo insmod ../bjackson-e1000e/e1000e-3.3.4/src/e1000e.ko
$ sudo insmod ../bjackson-ecnl/src/ecnl_device.ko


Make the driver interface code in C:
$ cd ecnl
ecnl$ make

$ cd ..

There are three executables.
$ cargo run --bin simulator --features="simulator"
$ cargo run --bin cell --features="cell"
$ cargo run --bin external_echoDemo --features="noc"

Tests are in src/test.rs
$ cargo test --features="simulator"
