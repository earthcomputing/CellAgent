Assuming this file is in cellagent directory of CellAgent repository.

An environment variable CELL_AGENT_DIR must be set.
An environment variable SERVER_URL must be set if you want to send trace records to the web server.

To load the new driver:
Edit (using sudo) /etc/modprobe.d/e1000e.conf to uncomment "blacklist e1000e".
sudo depmod -ae && sudo update-initramfs -u
Then reboot.  RECOMMENT THE LINE IN /etc/modprobe.d/e1000e.conf AND RERUN ABOVE LINE WHEN DONE!

After turning on cell:
$ sudo insmod ../../bjackson-e1000e/e1000e-3.3.4/src/e1000e.ko
$ sudo insmod ../../bjackson-ecnl/src/ecnl_device.ko


Make the driver interface code in C:
$ cd ecnl
ecnl$ make

$ cd ..

There are three executables in multicell/cellagent.
$ cargo run --bin simulator --features="simulator" -- config_file_name
$ cargo run --bin cell --features="cell"
$ cargo run --bin external_echoDemo --features="noc"

There are currently 3 configuration files in multicell/cellagent/configs.

If you want to display the simulator output in a browser,
from multicall/actix_server
$ cargo run
and from multicell/cellagent
$ cargo run --bin simulator --features="webserver simulator"  -- config_file_name

You MUST start the web server before the simulator.

Point a browser at SERVER_URL.

The server can replay from a trace file, two of which are in multicell/actix_server.

Tests are in src/test.rs
$ cargo test --features="simulator"
