Assuming this file is in top level of CellAgent repository.
An environment variable CELL_AGENT_DIR must be set.

After turning on cell:
$ sudo insmod ../NALDD/entl_drivers/ecnl_device/ecnl_device.ko


Make the driver interface code in C:
$ cd ecnl
ecnl$ make

$ cd ..

There are three executables.
$ cargo run --bin simulator --features="simulator" --manifest-path="binaries/simulator/Cargo.toml"
$ cargo run --bin cell --features="cell" --manifest-path="binaries/cell/Cargo.toml"
$ cargo run --bin external_echoDemo --features="noc" --manifest-path="binaries/external_echoDemo/Cargo.toml"

Tests are in src/test.rs
$ cargo test --features="simulator"
